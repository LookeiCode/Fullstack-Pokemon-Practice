const express = require('express');
const router = express.Router();

// or you can do --> const router = require('express').Router();
// This combines both L1-2 into 1 line together (short form)






module.exports = router;